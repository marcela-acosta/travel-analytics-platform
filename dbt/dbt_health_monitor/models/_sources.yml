version: 2

sources:
  - name: bronze
    description: Bronze event tables loaded in BigQuery.
    database: "{{ target.project }}"
    schema: "{{ var('bronze_dataset') }}"
    tables:
      - name: booking_events
        description: Raw booking events from source systems.
      - name: cancellation_events
        description: Raw cancellation events from source systems.
      - name: crm_events
        description: Raw CRM events from source systems.
      - name: customer_events
        description: Raw customer events from source systems.
      - name: flight_events
        description: Raw flight events from source systems.
      - name: hotel_events
        description: Raw hotel events from source systems.
      - name: marketing_events
        description: Raw marketing events from source systems.
      - name: payment_events
        description: Raw payment events from source systems.
      - name: review_events
        description: Raw review events from source systems.
      - name: search_events
        description: Raw search events from source systems.
      - name: support_events
        description: Raw support events from source systems.
      - name: agency_events
        description: Raw agency events from source systems.
      - name: baggage_events
        description: Raw baggage events from source systems.
      - name: device_events
        description: Raw device events from source systems.
      - name: fraud_events
        description: Raw fraud events from source systems.
      - name: insurance_events
        description: Raw insurance events from source systems.
      - name: invoice_events
        description: Raw invoice events from source systems.
      - name: itinerary_events
        description: Raw itinerary events from source systems.
      - name: location_events
        description: Raw location events from source systems.
      - name: loyalty_events
        description: Raw loyalty events from source systems.
      - name: notification_events
        description: Raw notification events from source systems.
      - name: partner_events
        description: Raw partner events from source systems.
      - name: promo_events
        description: Raw promo events from source systems.
      - name: refund_events
        description: Raw refund events from source systems.
      - name: seat_events
        description: Raw seat events from source systems.
      - name: session_events
        description: Raw session events from source systems.
