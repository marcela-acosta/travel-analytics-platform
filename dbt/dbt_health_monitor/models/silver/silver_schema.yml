version: 2

models:
  - name: slv_booking_events
    description: Curated booking events with standardized types, normalized text values, and latest-record deduplication by booking.
    columns:
      - name: booking_event_sk
        description: Deterministic surrogate key based on complete source row content.
        tests:
          - not_null
          - unique
      - name: booking_id
        description: Booking identifier from the source system.
        tests:
          - not_null
          - unique
      - name: customer_id
        description: Customer identifier associated with the booking.
        tests:
          - not_null
      - name: booking_date
        description: Date when the booking was created in the source.
      - name: travel_start_date
        description: Planned travel start date.
      - name: travel_end_date
        description: Planned travel end date.
      - name: booking_status
        description: Normalized booking status in lowercase.
        tests:
          - not_null
      - name: total_amount
        description: Total booking amount in source currency.
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: ">= 0"
      - name: currency
        description: Uppercase ISO-like currency code.
      - name: destination_city
        description: Destination city for the booking.
      - name: destination_country
        description: Uppercase destination country value.
      - name: channel
        description: Normalized booking channel in lowercase.
      - name: created_at
        description: Source-created timestamp parsed to TIMESTAMP.
      - name: updated_at
        description: Source-updated timestamp parsed to TIMESTAMP.
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_crm_events
    description: Curated CRM events with standardized types, normalized attributes, and latest-record deduplication by opportunity.
    columns:
      - name: crm_event_sk
        description: Deterministic surrogate key based on complete source row content.
        tests:
          - not_null
          - unique
      - name: opportunity_id
        description: CRM opportunity identifier.
        tests:
          - not_null
          - unique
      - name: stage
        description: Normalized sales stage in lowercase.
        tests:
          - not_null
      - name: agent_id
        description: Responsible sales agent identifier.
      - name: product
        description: Product associated with the opportunity.
      - name: value
        description: Opportunity value amount.
        tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: ">= 0"
      - name: region
        description: Uppercase region value.
      - name: expected_close_date
        description: Expected close date parsed to DATE.
      - name: updated_at
        description: Source-updated timestamp parsed to TIMESTAMP.
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_customer_events
    description: Curated customer events with standardized types, normalized identity fields, and latest-record deduplication by customer.
    columns:
      - name: customer_event_sk
        description: Deterministic surrogate key based on complete source row content.
        tests:
          - not_null
          - unique
      - name: customer_id
        description: Customer identifier.
        tests:
          - not_null
          - unique
      - name: full_name
        description: Customer full name.
        tests:
          - not_null
      - name: email
        description: Customer email normalized to lowercase.
      - name: phone
        description: Customer phone number.
      - name: country
        description: Uppercase country value.
      - name: city
        description: Customer city.
      - name: customer_segment
        description: Normalized customer segment in lowercase.
      - name: loyalty_tier
        description: Normalized loyalty tier in lowercase.
      - name: created_at
        description: Source-created timestamp parsed to TIMESTAMP.
      - name: updated_at
        description: Source-updated timestamp parsed to TIMESTAMP.
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_cancellation_events
    description: Curated cancellation events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: cancellation_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged cancellation event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_flight_events
    description: Curated flight events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: flight_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged flight event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_hotel_events
    description: Curated hotel events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: hotel_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged hotel event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_marketing_events
    description: Curated marketing events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: marketing_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged marketing event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_payment_events
    description: Curated payment events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: payment_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged payment event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_review_events
    description: Curated review events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: review_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged review event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_search_events
    description: Curated search events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: search_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged search event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_support_events
    description: Curated support events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: support_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged support event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_agency_events
    description: Curated agency events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: agency_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged agency event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_baggage_events
    description: Curated baggage events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: baggage_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged baggage event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_device_events
    description: Curated device events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: device_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged device event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_fraud_events
    description: Curated fraud events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: fraud_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged fraud event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_insurance_events
    description: Curated insurance events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: insurance_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged insurance event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_invoice_events
    description: Curated invoice events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: invoice_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged invoice event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_itinerary_events
    description: Curated itinerary events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: itinerary_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged itinerary event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_location_events
    description: Curated location events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: location_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged location event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_loyalty_events
    description: Curated loyalty events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: loyalty_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged loyalty event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_notification_events
    description: Curated notification events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: notification_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged notification event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_partner_events
    description: Curated partner events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: partner_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged partner event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_promo_events
    description: Curated promo events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: promo_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged promo event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_refund_events
    description: Curated refund events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: refund_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged refund event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_seat_events
    description: Curated seat events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: seat_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged seat event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null

  - name: slv_session_events
    description: Curated session events with stable surrogate keys and deduplicated latest records.
    columns:
      - name: session_event_sk
        description: Deterministic surrogate key generated from the full source row payload.
        tests:
          - not_null
          - unique
      - name: event_payload
        description: JSON payload snapshot of the staged session event.
        tests:
          - not_null
      - name: ingested_at
        description: Ingestion timestamp from bronze.
        tests:
          - not_null
      - name: staged_at
        description: Timestamp when the row was transformed in the staging layer.
        tests:
          - not_null
